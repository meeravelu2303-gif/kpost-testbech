node_modules/
dist/
reports/
playwright-report/
test-results/

# Every env file except the documented template. A committed credential is not
# recoverable from a public repo.
.env
.env.*
!.env.example
*.log

# Run-scoped session cache (bearer tokens). Never committed.
.auth/


# Per-run defect report, regenerated by the publish reporter each run.
BUG_REPORT.json
BUG_REPORT.md
